# Cloudflare Pages 配置文件
# 用于 Cloudflare Pages 部署

name = "ai-skill-tree-assessment"
compatibility_date = "2024-01-01"

# 注意：Cloudflare Pages 通过 Web UI 配置构建设置
# 构建命令：npm run build
# 输出目录：dist
# Node 版本：18 或更高

# 环境变量（如果需要）
# [env.production.vars]
# NODE_ENV = "production"

