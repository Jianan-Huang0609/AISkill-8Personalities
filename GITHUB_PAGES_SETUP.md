# GitHub Pages 部署配置指南

## 🎯 问题诊断

GitHub Pages 显示空白页的常见原因：

1. **Base 路径配置错误** - Vite 需要知道部署在子路径下
2. **缺少 404.html** - SPA 路由需要回退到 index.html
3. **GitHub Pages 设置未启用** - 需要在仓库设置中启用
4. **构建产物路径错误** - 需要从 `dist/` 目录部署

---

## ✅ 已完成的配置

### 1. GitHub Actions 工作流

已创建 `.github/workflows/deploy.yml`，会自动：
- 构建项目
- 部署到 GitHub Pages

### 2. Vite 配置

已更新 `vite.config.ts`，添加了 base 路径配置：
```typescript
base: process.env.GITHUB_PAGES === 'true'
  ? '/AISkill-8Personalities/'
  : '/'
```

### 3. 404.html 文件

已创建 `404.html`，用于 SPA 路由回退。

---

## 🚀 部署步骤

### 步骤 1: 启用 GitHub Pages

1. 进入 GitHub 仓库：`Jianan-Huang0609/AISkill-8Personalities`
2. 点击 **Settings**（设置）
3. 左侧菜单找到 **Pages**
4. 在 **Source** 部分：
   - 选择 **GitHub Actions**（不是 `main` 分支）
5. 保存

### 步骤 2: 推送代码触发部署

```bash
git add .
git commit -m "feat: 配置GitHub Pages部署"
git push origin main
```

### 步骤 3: 等待部署完成

1. 进入仓库的 **Actions** 标签
2. 查看 "Deploy to GitHub Pages" 工作流
3. 等待构建和部署完成（通常 2-5 分钟）

### 步骤 4: 访问网站

部署成功后，访问：
```
https://jianan-huang0609.github.io/AISkill-8Personalities/
```

---

## 🔧 如果仍然显示空白

### 检查 1: 浏览器控制台错误

打开浏览器开发者工具（F12），查看 Console 标签：
- 如果有 404 错误（找不到 JS/CSS 文件），说明 base 路径配置有问题
- 如果有其他错误，根据错误信息修复

### 检查 2: 网络请求

在 Network 标签中检查：
- `index.html` 是否成功加载（200）
- JS/CSS 文件是否成功加载
- 如果文件路径是 `/assets/xxx.js` 而不是 `/AISkill-8Personalities/assets/xxx.js`，说明 base 路径未生效

### 检查 3: 手动触发部署

如果自动部署失败：
1. 进入 **Actions** 标签
2. 选择 "Deploy to GitHub Pages" 工作流
3. 点击 **Run workflow**
4. 选择 `main` 分支
5. 点击 **Run workflow**

---

## 📝 配置说明

### Base 路径

GitHub Pages 部署在子路径下：`/AISkill-8Personalities/`

所以需要：
- Vite 构建时设置 `base: '/AISkill-8Personalities/'`
- 所有资源路径会自动加上这个前缀

### 404.html

GitHub Pages 不支持 `_redirects` 文件，所以使用 `404.html` 实现 SPA 路由回退：
- 当访问不存在的路径时，GitHub Pages 会返回 `404.html`
- `404.html` 中的脚本会将所有请求重定向到 `index.html`

---

## 🆘 故障排除

### 问题 1: 资源文件 404

**症状**: JS/CSS 文件加载失败

**解决**:
1. 检查 `vite.config.ts` 中的 base 配置
2. 确认构建时设置了 `GITHUB_PAGES=true` 环境变量
3. 重新构建和部署

### 问题 2: 路由不工作

**症状**: 直接访问 `/result` 等路由显示 404

**解决**:
1. 确认 `404.html` 文件已提交
2. 检查 `404.html` 中的路径是否正确（包含 `/AISkill-8Personalities/`）

### 问题 3: 部署失败

**症状**: GitHub Actions 工作流失败

**解决**:
1. 查看 Actions 日志，找到具体错误
2. 检查 `package.json` 中的脚本是否正确
3. 确认所有依赖都已正确安装

---

## ✅ 验证清单

部署前确认：
- [ ] `.github/workflows/deploy.yml` 已创建
- [ ] `vite.config.ts` 中配置了 base 路径
- [ ] `404.html` 文件已创建
- [ ] GitHub Pages 设置中选择了 **GitHub Actions**
- [ ] 代码已推送到 `main` 分支

部署后验证：
- [ ] 访问 `https://jianan-huang0609.github.io/AISkill-8Personalities/` 可以看到页面
- [ ] 浏览器控制台没有错误
- [ ] 所有资源文件（JS/CSS）都成功加载
- [ ] 路由正常工作（如访问 `/result`）

---

## 📚 参考文档

- [GitHub Pages 文档](https://docs.github.com/en/pages)
- [Vite 部署指南](https://vitejs.dev/guide/static-deploy.html#github-pages)
- [GitHub Actions 文档](https://docs.github.com/en/actions)

