# Cloudflare Pages 正确设置指南

## ⚠️ 重要：区分 Workers 和 Pages

你当前看到的是 **Cloudflare Workers** 的设置页面，但我们需要的是 **Cloudflare Pages**。

### Workers vs Pages

- **Workers**: 用于运行服务器端代码（需要 `wrangler deploy`）
- **Pages**: 用于部署静态网站（自动部署，不需要 `wrangler deploy`）

我们的项目是静态网站（Vite + React），应该使用 **Pages**。

---

## 🚀 创建 Cloudflare Pages 项目

### 步骤 1: 进入 Pages 页面

1. 在 Cloudflare Dashboard 左侧菜单中
2. 点击 **"Workers 和 Pages"** (Workers and Pages)
3. 点击 **"Pages"** 标签（不是 Workers）
4. 点击 **"Create a project"** (创建项目)

### 步骤 2: 连接 Git 仓库

1. 选择 **"Connect to Git"**
2. 授权 Cloudflare 访问你的 GitHub（如果还没授权）
3. 选择仓库：`Jianan-Huang0609/AISkill-8Personalities`

### 步骤 3: 配置构建设置

在项目设置页面填写：

- **Project name**: `ai-skill-tree-assessment` (或你喜欢的名称)
- **Production branch**: `main`
- **Framework preset**: **Vite** (重要！)
- **Build command**: `npm run build`
- **Build output directory**: `dist`
- **Root directory**: `/` (留空或填写 `/`)
- **Deploy command**: ⚠️ **留空或删除**（不要设置任何值！）

### 步骤 4: 保存并部署

1. 点击 **"Save and Deploy"**
2. 等待构建完成（通常 2-5 分钟）

---

## ❌ 如果误创建了 Workers 项目

如果你已经在 Workers 中创建了项目，有两个选择：

### 选择 1: 删除 Workers 项目，创建 Pages 项目（推荐）

1. 删除当前的 Workers 项目
2. 按照上面的步骤创建新的 Pages 项目

### 选择 2: 修复当前 Workers 配置（不推荐）

如果你想继续使用 Workers（不推荐，因为这是静态网站）：

1. 在弹窗中，将 **"部署命令"** 字段留空
2. 点击 **"更新"**
3. 但这样仍然不是最佳实践，因为静态网站应该用 Pages

---

## ✅ 验证部署

部署成功后，你会得到一个 Pages URL：
- 格式：`https://your-project-name.pages.dev`
- 可以在项目设置中自定义域名

---

## 📋 正确的配置对比

### Cloudflare Pages（正确）✅

```
项目类型: Pages
框架预设: Vite
构建命令: npm run build
输出目录: dist
部署命令: (留空)
```

### Cloudflare Workers（错误）❌

```
项目类型: Workers
构建命令: npm run build:ci
部署命令: npx wrangler deploy  ← 这是问题所在
```

---

## 🔍 如何确认你在 Pages 而不是 Workers

在 Cloudflare Dashboard 中：

- ✅ **Pages**: URL 包含 `/pages/`，左侧菜单显示 "Pages" 项目
- ❌ **Workers**: URL 包含 `/workers/`，左侧菜单显示 "Workers" 项目

---

## 💡 为什么用 Pages 而不是 Workers？

1. **静态网站**: 我们的项目是纯前端应用，不需要服务器端代码
2. **自动部署**: Pages 会自动处理部署，不需要手动运行命令
3. **全球 CDN**: Pages 提供更好的静态资源分发
4. **更简单**: 不需要配置 Workers 脚本

---

## 🆘 如果还有问题

如果按照上述步骤操作后仍有问题，请检查：

1. 确认创建的是 **Pages** 项目，不是 Workers
2. 确认 **"部署命令"** 字段为空
3. 确认构建命令是 `npm run build`（不是 `npm run build:ci`）
4. 查看构建日志中的具体错误信息

