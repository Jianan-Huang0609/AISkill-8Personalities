# Cloudflare Workers 自定义域名配置指南

## 🎯 问题说明

`*.workers.dev` 域名在某些地区（包括中国大陆）可能无法稳定访问。解决方案是**绑定自定义域名**。

---

## ✅ 解决方案：绑定自定义域名

### 前提条件

1. **拥有一个域名**（可以在 Cloudflare 购买，或使用已有域名）
2. **域名已添加到 Cloudflare**（作为 DNS 提供商）

---

## 📋 配置步骤

### 步骤 1: 在 Cloudflare Dashboard 中添加域名

1. 登录 [Cloudflare Dashboard](https://dash.cloudflare.com)
2. 点击右上角 **"Add a site"**（添加站点）
3. 输入你的域名（如 `example.com`）
4. 选择免费计划（Free plan）
5. 按照提示更新域名的 DNS 服务器到 Cloudflare

### 步骤 2: 在 Workers 项目中绑定自定义域名

1. 进入你的 Workers 项目：`aiskill-8personalities`
2. 点击 **"Settings"**（设置）标签
3. 找到 **"Triggers"**（触发器）部分
4. 点击 **"Add Custom Domain"**（添加自定义域名）
5. 输入你的域名（如 `ai-skill.yourdomain.com` 或 `yourdomain.com`）
6. 点击 **"Add Custom Domain"**

### 步骤 3: 配置 DNS 记录（如果需要）

Cloudflare 通常会自动创建 DNS 记录，但如果需要手动配置：

1. 进入域名的 **"DNS"** 设置
2. 添加一条 **CNAME** 记录：
   - **Name**: `ai-skill`（或你想要的子域名，如 `www`）
   - **Target**: `aiskill-8personalities.jiananh99.workers.dev`
   - **Proxy status**: ✅ Proxied（橙色云朵图标）

### 步骤 4: 等待 DNS 传播

- DNS 记录通常需要几分钟到几小时才能生效
- 可以在 Cloudflare Dashboard 中查看状态

---

## 🌐 推荐的域名配置

### 选项 1: 使用子域名（推荐）

```
ai-skill.yourdomain.com
```

**优点**：
- 不影响主域名
- 可以同时部署多个项目

### 选项 2: 使用主域名

```
yourdomain.com
www.yourdomain.com
```

**优点**：
- 更简洁的 URL
- 适合作为主要网站

---

## 🔍 验证配置

配置完成后，访问你的自定义域名：

```bash
# 测试访问
curl -I https://ai-skill.yourdomain.com

# 应该返回 200 OK
```

---

## 🆘 故障排除

### 问题 1: DNS 解析失败

**症状**: 域名无法解析

**解决**:
1. 检查 DNS 记录是否正确
2. 确认 Proxy status 是 **Proxied**（橙色云朵）
3. 等待 DNS 传播（最多 24 小时）

### 问题 2: SSL 证书未就绪

**症状**: 访问时显示 SSL 错误

**解决**:
1. Cloudflare 会自动为自定义域名生成 SSL 证书
2. 等待几分钟让证书生效
3. 在 Workers 设置中检查 SSL 状态

### 问题 3: 仍然无法访问

**症状**: 配置后仍然超时

**解决**:
1. 确认域名已正确绑定到 Workers
2. 检查 Workers 部署状态（应该是 Active）
3. 尝试清除浏览器缓存
4. 使用不同网络测试（移动网络 vs WiFi）

---

## 💡 免费域名选项

如果你还没有域名，可以考虑：

1. **Cloudflare Registrar**
   - 直接在 Cloudflare 购买域名
   - 价格透明，无隐藏费用
   - 自动配置 DNS

2. **其他域名注册商**
   - Namecheap、GoDaddy 等
   - 需要将 DNS 服务器改为 Cloudflare 的

---

## 📝 更新 wrangler.toml（可选）

如果你使用 CLI 部署，可以在 `wrangler.toml` 中添加路由配置：

```toml
name = "aiskill-8personalities"
compatibility_date = "2025-12-30"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"

# 自定义域名路由（可选）
# [[routes]]
# pattern = "ai-skill.yourdomain.com/*"
# zone_name = "yourdomain.com"
```

但通常通过 Dashboard 配置更简单。

---

## ✅ 配置完成后的优势

1. **国内可访问**: 自定义域名通过 Cloudflare CDN，国内访问更稳定
2. **更好的 SEO**: 使用自己的域名
3. **品牌一致性**: 使用自己的域名更专业
4. **SSL 证书**: Cloudflare 自动提供免费 SSL

---

## 🚀 快速开始

如果你已经有域名：

1. 确保域名在 Cloudflare 管理
2. 在 Workers 项目设置中添加自定义域名
3. 等待 DNS 和 SSL 生效（通常几分钟）
4. 访问你的自定义域名

如果还没有域名：

1. 在 Cloudflare 购买域名（或使用其他注册商）
2. 将域名添加到 Cloudflare
3. 按照上述步骤配置

---

## 📚 参考文档

- [Cloudflare Workers 自定义域名](https://developers.cloudflare.com/workers/configuration/routing/custom-domains/)
- [Cloudflare DNS 设置](https://developers.cloudflare.com/dns/)
- [Cloudflare SSL/TLS](https://developers.cloudflare.com/ssl/)

